{
  "name": "llvm-opt-viewer",
  "main": "lib/index.js",
  "devDependencies": {
    "@types/node": "^7.0.12",
    "ava": "^0.19.0",
    "eslint": "^3.19.0",
    "typescript": "^2.2.2"
  },
  "scripts": {
    "build": "tsc",
    "prepublish": "yarn run build",
    "test": "tsc && ava lib/test --verbose"
  },
  "dependencies": {
    "@types/ramda": "types/npm-ramda",
    "ramda": "^0.23.0",
    "yamljs": "^0.2.9"
  },
  "version": "0.0.1"
}
